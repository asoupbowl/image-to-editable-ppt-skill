# Observation slide AB test artifact payload

This directory stores a text-only base64 payload for `observation-slide-abtest.zip`.
GitHub Actions reconstructs the ZIP and uploads it as the `observation-slide-abtest` artifact.

Original ZIP SHA256:

```text
9b55299c43c2e77f4d750ddbda2565f19a9c61285ede0b52f99929b627ebb8fb
```

Expected ZIP entries, in order:

```text
observation-slide_edited.pptx
preview.png
validation.json
```
